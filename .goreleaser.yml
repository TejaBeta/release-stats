
   
# .goreleaser.yml
# Build customization
builds:
  - main: ./main.go
    binary: grs
    ldflags: -s -w -X github.com/TejaBeta/grs/cmd.version={{.Version}} -X github.com/TejaBeta/grs/cmd.commit={{.Commit}}
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
archives:
  - id: grs
    format: tar.gz
    replacements:
      amd64: x86_64
      darwin: Darwin
      linux: Linux
